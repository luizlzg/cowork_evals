---
name: python-version
description: A case reaches a running command, on the interpreter the backend put there.
tags: [plugin]
plugins: ["../../.."]
runs: 1
---

Run `python3 -V` and reply with its output and nothing else.
