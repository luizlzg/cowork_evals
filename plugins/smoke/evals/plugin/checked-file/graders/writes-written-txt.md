---
# The grader says the file appeared. The checks beside it say what is inside it, which is
# what no grader type can express. The harness also refuses a case carrying no grader at
# all, so a checks/ directory is added to a case and never replaces its graders/. See
# ../../../../../README.md.
type: file_exists
path: written.txt
---
