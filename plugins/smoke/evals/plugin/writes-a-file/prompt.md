---
name: writes-a-file
description: A case reaches the Write tool, so a run that was denied it can be told from one that had it.
tags: [plugin]
plugins: ["../../.."]
runs: 1
---

Write the single word WRITTEN into a file named `written.txt` in the working directory. Reply
with the word WRITTEN and nothing else.
