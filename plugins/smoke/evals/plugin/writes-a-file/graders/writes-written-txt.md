---
# The case writes no `allowed_tools`. `Write` comes from the operator grant alone, which is
# what makes this case say whether the run had it. eval.allow_tools is the session mirror in
# docs/running_evals.md.
type: file_exists
path: written.txt
---
