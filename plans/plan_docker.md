# Plan: the Docker backend

Branch `feat/docker`, cut from `main`.

**Not finished. This is a skeleton.** Scope and phases are being written.

## Scope

The container that reproduces the CoWork image, and the backend that runs the harness inside
it. The design is [`../docs/docker.md`](../docs/docker.md).

TBD.

## Phases

TBD.
